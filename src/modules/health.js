import { createAction, handleActions } from "redux-actions";
// 액션 추가시 추가



// 초기값
const initialValue = {
  previousUrl : ""
}


// 리듀서 추가시 추가
const health = createAction({


})

export default health;